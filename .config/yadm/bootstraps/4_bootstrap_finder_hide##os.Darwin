#!/bin/bash

set -euo pipefail

# macOS Finder ignores .hidden; use chflags hidden flag instead.
hide=(
	"${HOME}/LICENSE"
)

for path in "${hide[@]}"; do
	[ -e "${path}" ] || continue
	echo "Hiding ${path}"
	chflags hidden "${path}"
done
